package com.zgamelogic.data.database.apple.live;

import org.springframework.data.jpa.repository.JpaRepository;

public interface LiveActivityRepository extends JpaRepository<LiveActivity, String> {
}
